import hello from "./modulo.mjs"
hello()

//El import en este caso funciona al cambiar la extencion de los archivos a mjs
//tambien se puede ocupar modulos con la extencion js, solo que se tiene que modificar el package.json y agregar "type": "module"